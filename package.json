{
  "name": "college-qa-dashboard",
  "version": "1.0.0",
  "description": "Professional Q&A Dashboard for College",
  "main": "api/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd api && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd api && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd api && npm start"
  },
  "keywords": [
    "college",
    "qa",
    "dashboard",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "College Q&A Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
  }
}
